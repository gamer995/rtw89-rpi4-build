name: Build patched rtw89-8922au kmod for iStoreOS RPi4

on:
  workflow_dispatch:
  push:
    paths:
      - '.github/workflows/build-rtw89-kmod.yml'
      - 'package/kernel/rtw89-8922au-git/**'
      - 'README.md'

jobs:
  build:
    name: Build patched source inside OpenWrt mac80211 backports
    runs-on: ubuntu-22.04

    env:
      SDK_URL: https://downloads.openwrt.org/releases/24.10.5/targets/bcm27xx/bcm2711/openwrt-sdk-24.10.5-bcm27xx-bcm2711_gcc-13.3.0_musl.Linux-x86_64.tar.zst
      OPENWRT_REF: v24.10.5
      RTW89_REF: d2f175eafa0a4ef9cc65e7073a77e60238cae614
      KERNEL_ABI: 6.6.119~5642ee3ae5a6da3ce336f51cf968083c-r1
      OUT_VERSION: 6.6.119.2026.05.15~d2f175ea-r7
      PKG_NAME: kmod-rtw89-8922au-git

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install build dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y --no-install-recommends \
            build-essential clang flex bison g++ gawk gcc-multilib gettext \
            git libncurses5-dev libssl-dev python3-distutils rsync unzip \
            zstd file wget ca-certificates binutils-aarch64-linux-gnu

      - name: Download OpenWrt 24.10.5 bcm2711 SDK and mac80211 package
        run: |
          set -eu
          mkdir sdk openwrt-src
          wget -q "$SDK_URL" -O sdk.tar.zst
          tar --zstd -xf sdk.tar.zst -C sdk --strip-components=1
          git clone --depth 1 --branch "$OPENWRT_REF" https://github.com/openwrt/openwrt.git openwrt-src
          mkdir -p sdk/package/kernel
          cp -a openwrt-src/package/kernel/mac80211 sdk/package/kernel/

      - name: Prepare mac80211 backports tree
        run: |
          set -eu
          cd sdk
          cat >> .config <<'EOF'
          CONFIG_PACKAGE_kmod-cfg80211=m
          CONFIG_PACKAGE_kmod-mac80211=m
          CONFIG_PACKAGE_kmod-rtw89=m
          CONFIG_DRIVER_11AC_SUPPORT=y
          CONFIG_DRIVER_11AX_SUPPORT=y
          CONFIG_DRIVER_11BE_SUPPORT=y
          CONFIG_PACKAGE_MAC80211_DEBUGFS=y
          EOF
          make defconfig
          make package/kernel/mac80211/prepare V=s

      - name: Compile OpenWrt mac80211 backports
        run: |
          set -eu
          cd sdk
          make package/kernel/mac80211/compile V=s -j"$(nproc)"

      - name: Build morrownr rtw89 against compiled backports ABI
        run: |
          set -eu
          git clone https://github.com/morrownr/rtw89.git rtw89-src
          git -C rtw89-src checkout "$RTW89_REF"

          for p in package/kernel/rtw89-8922au-git/patches/*.patch; do
            git -C rtw89-src apply --check "$GITHUB_WORKSPACE/$p"
            git -C rtw89-src apply "$GITHUB_WORKSPACE/$p"
          done

          bp_dir=$(find sdk/build_dir -path '*/mac80211-regular/backports-6.12.61' -type d | head -1)
          test -n "$bp_dir"
          rm -rf "$bp_dir/drivers/net/wireless/realtek/rtw89"
          mkdir -p "$bp_dir/drivers/net/wireless/realtek"
          cp -a rtw89-src "$bp_dir/drivers/net/wireless/realtek/rtw89"

          echo "Backports tree: $bp_dir"
          grep -R "skip MAC flush for USB" -n "$bp_dir/drivers/net/wireless/realtek/rtw89" || true
          grep -R "extend RF DACK wait" -n "$bp_dir/drivers/net/wireless/realtek/rtw89"

          cd sdk
          bp_dir=$(find build_dir -path '*/mac80211-regular/backports-6.12.61' -type d | head -1)
          linux_dir=$(find build_dir -path '*/linux-bcm27xx_bcm2711/linux-6.6.119' -type d | head -1)
          symvers=$(find build_dir -path '*/symvers/mac80211.symvers' -type f | head -1)
          toolchain_bin=$(find staging_dir -path '*/bin/aarch64-openwrt-linux-musl-gcc' -type f | head -1)
          test -n "$bp_dir"
          test -n "$linux_dir"
          test -n "$symvers"
          test -n "$toolchain_bin"
          export PATH="$(dirname "$toolchain_bin"):$PATH"
          make -C "$bp_dir" -f Makefile.real \
            KCFLAGS="-ffile-prefix-map=$PWD/build_dir/target-aarch64_cortex-a72_musl=target-aarch64_cortex-a72_musl -fno-caller-saves " \
            CROSS_COMPILE="aarch64-openwrt-linux-musl-" \
            ARCH="arm64" \
            KBUILD_HAVE_NLS=no \
            KBUILD_BUILD_USER="builder" \
            KBUILD_BUILD_HOST="buildhost" \
            KBUILD_BUILD_TIMESTAMP="Wed Dec 17 21:08:22 2025" \
            KBUILD_BUILD_VERSION="0" \
            CONFIG_SHELL="bash" \
            V=1 \
            cmd_syscalls= \
            KBUILD_EXTRA_SYMBOLS="$PWD/$symvers" \
            CC="aarch64-openwrt-linux-musl-gcc" \
            EXTRA_CFLAGS="-I$PWD/$bp_dir/include -ffile-prefix-map=$PWD/$bp_dir=backports-6.12.61 " \
            KLIB_BUILD="$PWD/$linux_dir" \
            MODPROBE=true \
            KLIB=/lib/modules/6.6.119 \
            KERNEL_SUBLEVEL=6 \
            KBUILD_LDFLAGS_MODULE_PREREQ= \
            modules
          find "$bp_dir/drivers/net/wireless/realtek/rtw89" -maxdepth 1 -name 'rtw89*_git.ko' -print -exec ls -lh {} \;

      - name: Package and verify iStoreOS modules
        run: |
          set -eu
          rm -rf out verify/data verify/control
          mkdir -p out verify/data/lib/modules/6.6.119 verify/control

          bp_dir=$(find sdk/build_dir -path '*/mac80211-regular/backports-6.12.61' -type d | head -1)
          rtw_dir="$bp_dir/drivers/net/wireless/realtek/rtw89"
          find "$rtw_dir" -maxdepth 1 -name '*.ko' -print | sort

          for ko in \
            rtw89_core_git.ko \
            rtw89_usb_git.ko \
            rtw89_8922a_git.ko \
            rtw89_8922au_git.ko
          do
            ko_path="$rtw_dir/$ko"
            test -s "$ko_path"
            cp "$ko_path" "verify/data/lib/modules/6.6.119/$ko"
            size_hex=$(aarch64-linux-gnu-objdump -h "$ko_path" \
              | awk '$2 == ".gnu.linkonce.this_module" { print $3 }')
            test "$size_hex" = "00000280"
            echo "$ko: .gnu.linkonce.this_module=0x${size_hex}"
          done

          strings verify/data/lib/modules/6.6.119/rtw89_core_git.ko \
            | grep -qx 'depends=mac80211,cfg80211'
          strings verify/data/lib/modules/6.6.119/rtw89_usb_git.ko \
            | grep -qx 'depends=rtw89_core_git,mac80211'
          strings verify/data/lib/modules/6.6.119/rtw89_8922a_git.ko \
            | grep -qx 'depends=rtw89_core_git,cfg80211'
          strings verify/data/lib/modules/6.6.119/rtw89_8922au_git.ko \
            | grep -qx 'depends=rtw89_8922a_git,rtw89_usb_git'

          for ko in verify/data/lib/modules/6.6.119/*.ko; do
            echo "$(basename "$ko") modinfo:"
            strings "$ko" | grep -E '^(name=|depends=|vermagic=)' | sort
          done

          {
            echo "Package: ${PKG_NAME}"
            echo "Version: ${OUT_VERSION}"
            echo "Depends: kernel (=${KERNEL_ABI}), kmod-cfg80211, kmod-mac80211, kmod-usb-core"
            echo "Source: package/kernel/rtw89-8922au-git"
            echo "SourceName: rtw89-8922au-git"
            echo "License: GPL-2.0 OR BSD-3-Clause"
            echo "Section: kernel"
            echo "Architecture: aarch64_cortex-a72"
            echo "Installed-Size: $(du -k -s verify/data/lib | awk '{print $1}')"
            echo "Description: Realtek RTL8912AU/RTL8922AU USB driver patched for iStoreOS RPi4 USB AP stability"
          } > verify/control/control

          tar -czf control.tar.gz -C verify/control .
          tar -czf data.tar.gz -C verify/data .
          printf '2.0\n' > debian-binary
          out_ipk="out/${PKG_NAME}_${OUT_VERSION}_aarch64_cortex-a72.ipk"
          tar -czf "$out_ipk" ./debian-binary ./control.tar.gz ./data.tar.gz
          echo "OUT_IPK=$out_ipk" >> "$GITHUB_ENV"
          ls -lh "$out_ipk"

      - name: Upload patched .ipk artifact
        uses: actions/upload-artifact@v4
        with:
          name: kmod-rtw89-8922au-patched-r7-backports-istoreos-5642ee
          path: out/*.ipk
          retention-days: 14

      - name: Show build summary
        run: |
          {
            echo '## Patched r7 package'
            echo ''
            echo '- Target: iStoreOS 24.10.5 Raspberry Pi 4, kernel 6.6.119'
            echo '- Build path: OpenWrt mac80211 backports-6.12.61 package, with morrownr/rtw89 substituted into the backports rtw89 directory'
            echo '- Kernel ABI normalized to: 6.6.119~5642ee3ae5a6da3ce336f51cf968083c-r1'
            echo '- Source: morrownr/rtw89 d2f175eafa0a4ef9cc65e7073a77e60238cae614'
            echo '- Patches: USB MAC flush skip, RTL8922A USB DACK wait extension'
            echo "- Output: ${OUT_IPK}"
          } >> "$GITHUB_STEP_SUMMARY"
